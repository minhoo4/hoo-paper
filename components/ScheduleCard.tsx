"use client";

type ScheduleCardProps = {};

export default function ScheduleCard({
}: ScheduleCardProps) {
  return (
    <article className="relative overflow-hidden rounded-3xl">
      {/* 다음 단계에서 일정 카드가 들어갈 자리 */}
    </article>
  );
}